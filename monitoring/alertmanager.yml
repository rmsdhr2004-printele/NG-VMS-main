global:
  resolve_timeout: 5m
  # Enterprise SMTP Configuration (Default setup)
  # smtp_smarthost: 'smtp.client.com:587'
  # smtp_from: 'alertmanager@client.com'
  # smtp_auth_username: 'alertmanager'
  # smtp_auth_password: 'password'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'enterprise_alerts'

receivers:
  - name: 'enterprise_alerts'
    # Optional Slack Webhook integration (highly popular with clients)
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/T00/B00/X00'
    #     channel: '#vms-alerts'
    #     send_resolved: true
    #     title: '[[.Status | toUpper]] NG-VMS System Alert'
    #     text: 'Alert: {{ .CommonAnnotations.summary }}\nDescription: {{ .CommonAnnotations.description }}'

    # Optional Email notification integrations
    # email_configs:
    #   - to: 'admin@client.com'
    #     send_resolved: true

