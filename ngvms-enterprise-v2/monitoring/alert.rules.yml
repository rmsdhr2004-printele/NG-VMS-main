groups:
  - name: ngvms_alerts
    rules:
      - alert: BackendDown
        expr: up{job="ngvms_backend"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Backend is down"
          description: "The NG-VMS Backend service has been unreachable for more than 1 minute."
